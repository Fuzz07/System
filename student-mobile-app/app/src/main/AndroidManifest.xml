<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SSCStudentApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- FCM Service -->
        <service
            android:name=".SSCMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM: Default notification channel for background / killed-app notifications -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="ssc_notifications" />
        <!-- FCM: Default notification icon (shown in status bar when app is killed) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher" />
        <!-- FCM: Default notification color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/indigo_600" />

        
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SSCStudentApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:theme="@style/Theme.SSCStudentApp.NoActionBar" />

        <activity
            android:name=".AnnouncementsActivity"
            android:exported="false"
            android:theme="@style/Theme.SSCStudentApp.NoActionBar" />

        <activity
            android:name=".ChatbotActivity"
            android:exported="false"
            android:theme="@style/Theme.SSCStudentApp.NoActionBar" />

        <activity
            android:name=".OfflineActivity"
            android:exported="false"
            android:theme="@style/Theme.SSCStudentApp.NoActionBar" />
    </application>

</manifest>
